"use strict";
// Создание изменяемой переменной
let number = 5;

// Вывод в консоль
console.log(number/0);
console.log('str'*9);

// Создание неизменяемой переменной
const person = "Alex";

const bool = false;

// создание переменной undefined
let und;
console.log(und);

// Вроде словарь
const obj = {
    name: "John",
    age: 34,
    isMaried: false,
};

console.log(obj.name);
console.log(obj['age']);


// массив 
let arr = ['plum.png', 'orange.jpg', 'apple.png'];
console.log(arr[0]);
